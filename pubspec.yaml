name: flutter_quality_analyzer
description: A CLI tool that analyzes Flutter project dependencies — version checks, license detection, test coverage, and AI-powered summary using Gemini.
version: 2.0.1
homepage: https://github.com/YOUR_REAL_USERNAME/flutter_quality_analyzer
repository: https://github.com/YOUR_REAL_USERNAME/flutter_quality_analyzer

environment:
  sdk: ">=3.0.0 <4.0.0"

executables:
  fqa: flutter_quality_analyzer

dependencies:
  args: ^2.4.2
  http: ^1.2.1
  yaml: ^3.1.2
  pub_semver: ^2.1.4
  ansi_styles: ^0.3.1

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
