name: flutter_quality_analyzer
description: A CLI tool to analyze Flutter project dependencies and check for outdated packages.
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.4.2
  http: ^1.2.1
  yaml: ^3.1.2
  pub_semver: ^2.1.4
  ansi_styles: ^0.3.1

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
